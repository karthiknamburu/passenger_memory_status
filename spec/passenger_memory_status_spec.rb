require 'spec_helper'

describe PassengerMemoryStatus do
  it 'has a version number' do
    expect(PassengerMemoryStatus::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
